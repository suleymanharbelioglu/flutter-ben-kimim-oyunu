import 'dart:ui';

class AppColors {
  static const Color primary = Color(
    0xFF6C5CE7,
  ); // Mor ton, başlıklar ve butonlar
  static const Color secondary = Color(
    0xFF00B894,
  ); // Canlı yeşil, olumlu durumlar
  static const Color accent = Color(
    0xFFFF7675,
  ); // Soft kırmızı/pembe, uyarılar veya vurgular
  static const Color tertiary = Color(
    0xFFFFA500,
  ); // Turuncu ton, üçüncü buton için
}
